-if flash.present?
  -fetch_flashes.each do |key,value|
    .alert.alert-dismissible{class: "alert-#{flash_class(key)}"}
      %button.close{"data-dismiss" => "alert", "aria-label" => "Close"}×
      =value